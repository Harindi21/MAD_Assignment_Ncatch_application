import 'package:flutter/material.dart';

class Fpassword extends StatelessWidget {
  const Fpassword({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('DO something on this page')),
    );
  }
}
