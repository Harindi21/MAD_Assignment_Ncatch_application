import 'package:flutter/material.dart';

class Log_in extends StatelessWidget {
  const Log_in({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('yess it is working')),
    );
  }
}
